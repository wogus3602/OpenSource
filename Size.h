#ifndef _SCOREANDSCHEME_H
/*score 변수와 scheme 변수를 전역변수로 사용하기 위한 헤더 파일이다.*/
#define _SCOREANDSCHEME_H

#include <stdint.h>

extern uint32_t score; //score를 전역변수로 활용
extern uint8_t scheme; //scheme를 전역변수로 활용

#endif
